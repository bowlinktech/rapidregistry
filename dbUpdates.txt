CREATE TABLE `rapidregistry`.`survey_categories` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `programId` INT NOT NULL,
  `categoryName` VARCHAR(45) NOT NULL,
  `dateCreated` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`));

ALTER TABLE `rapidregistry`.`survey_categories` 
ADD INDEX `programId` (`programId` ASC);

CREATE TABLE `rapidregistry`.`survey_category_assoc` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `programId` INT NOT NULL,
  `surveyId` INT NOT NULL,
  `categoryId` INT NOT NULL,
  `dateCreated` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`));

ALTER TABLE `rapidregistry`.`survey_questions` 
ADD COLUMN `populateFromSurvey` INT NULL DEFAULT '0' AFTER `customerQNum`,
ADD COLUMN `populateFromSurveyQuestion` INT NULL DEFAULT '0' AFTER `populateFromSurvey`;

